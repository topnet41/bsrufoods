import 'dart:io';
import 'dart:math';
import 'package:firebase_storage/firebase_storage.dart';

class RegisterContro{
  Future<void> uploadPictureToStore(File file)async{
      // Random random = Random();
      // int i = random.nextInt(100000);
  
      // FirebaseStorage firebaseStorage = FirebaseStorage.instance;
      // var uploadUrl = await firebaseStorage.ref('Member/member$i.jpg').putFile(file);

      print("asd");


  }
}